import {
  create,
  getAll,
  getById,
  setArchived,
  setComplete,
} from "./TaskService";
import { deserialize } from "./TaskFactory";
import { transform } from "./TaskTransformer";

export {
  create,
  getAll,
  getById,
  setArchived,
  setComplete,
  deserialize,
  transform,
};
